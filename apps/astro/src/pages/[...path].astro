---
import { storePageData } from '../pages.js';
import { Frame } from '@custom/ui/Routes/Frame.js';
import { Page } from '@custom/ui/Routes/Page.js';

export { getStaticPaths } from '../pages.js';

const { path } = Astro.params;
await storePageData(path, Astro.props);
---

<Frame><Page path={path} /></Frame>
