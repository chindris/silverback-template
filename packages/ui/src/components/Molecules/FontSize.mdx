import { Meta, Unstyled } from '@storybook/blocks';
import tailwindConfig from '../../stylingAssets.json';

<Meta title="Theme/Styling & Theming/Font Size" />

export const SampleText =
  'Lorem ipsum dolor sit amet, consectetur adipiscing elit.';

# Font Size

{tailwindConfig.theme?.fontSize ? (

<Unstyled>
  {Object.entries(tailwindConfig.theme.fontSize).map(([key, value]) => {
    return (
      <div style={{ margin: '5rem 0' }}>
        <span style={{ color: '#888' }}>{`text-${key}`}</span>
        <p style={{ fontSize: value }}>{SampleText}</p>
      </div>
    );
  })}
</Unstyled>
) : <span>There are no font sizes defined in the tailwind config</span>}
