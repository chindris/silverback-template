import { Meta, Unstyled } from '@storybook/blocks';
import tailwindConfig from '../../stylingAssets.json';

<Meta title="Theme/Styling & Theming/Line Height" />

export const SampleText =
  'Lorem ipsum dolor sit amet, consectetur adipiscing elit. Integer consequat varius finibus. Class aptent taciti sociosqu ad litora torquent per conubia nostra, per inceptos himenaeos. Ut porta euismod purus quis imperdiet. Nulla aliquam suscipit vehicula. Maecenas pulvinar justo sit amet elit venenatis venenatis. Maecenas fermentum nisl eu est hendrerit finibus.';

# Line Height

{tailwindConfig.theme?.lineHeight ? (

<Unstyled>
  {Object.entries(tailwindConfig.theme.lineHeight).map(([key, value]) => {
    return (
      <div style={{ margin: '5rem 0' }}>
        <span style={{ color: '#888' }}>{`leading-${key}`}</span>
        <p style={{ lineHeight: value }}>{SampleText}</p>
      </div>
    );
  })}
</Unstyled>
) : <span>There are no line heights defined in the tailwind config</span>}
