import { Meta, StoryObj } from '@storybook/react';

import { Loading } from './Loading';

export default {
  component: Loading,
} satisfies Meta<typeof Loading>;

export const LoadingStory = {} satisfies StoryObj<typeof Loading>;
