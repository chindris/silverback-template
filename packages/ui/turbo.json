{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "pipeline": {
    "prep:scripts": {
      "dependsOn": ["^prep"],
      "inputs": [
        "src/**/*.ts",
        "src/**/*.tsx",
        ".swcrc",
        "tsconfig.json",
        "static/public/**"
      ],
      "outputs": ["build/**/*.js", "build/**/*.d.ts"]
    },
    "prep:styles": {
      "inputs": [
        "postcss.config.cjs",
        "tailwind.config.cjs",
        "tailwind.css",
        "src/**/*.css",
        "src/components/**/*tsx"
      ],
      "outputs": ["build/styles.css"]
    },
    "prep:i18n": {
      "inputs": ["src/components/**/*.tsx"],
      "outputs": ["build/translatables.json"]
    },
    "prep": {
      "dependsOn": ["prep:scripts", "prep:styles", "prep:i18n"]
    },
    "build": {
      "inputs": [
        ".storybook/**",
        "postcss.config.cjs",
        "tailwind.config.cjs",
        "tailwind.css",
        "src/**/*.css",
        "src/**/*.tsx",
        "src/**/*.ts",
        "static/**"
      ],
      "outputs": ["storybook-static/**"]
    },
    "test:integration": {
      "outputs": ["coverage/**"]
    }
  }
}
