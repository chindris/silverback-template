{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build:scripts": {
      "dependsOn": ["^prep"],
      "inputs": [
        "src/**/*.ts",
        "src/**/*.tsx",
        ".swcrc",
        "tsconfig.json",
        "static/public/**"
      ],
      "outputs": ["build/**/*.js", "build/**/*.d.ts"]
    },
    "build:styles": {
      "inputs": [
        "postcss.config.cjs",
        "tailwind.config.cjs",
        "tailwind.css",
        "src/**/*.css",
        "src/components/**/*tsx"
      ],
      "outputs": ["build/styles.css"]
    },
    "build:i18n": {
      "inputs": ["src/components/**/*.tsx"],
      "outputs": ["build/translatables.json"]
    },
    "prep": {
      "dependsOn": ["build:scripts", "build:styles", "build:i18n"]
    },
    "build": {
      "inputs": [
        ".storybook/**",
        "postcss.config.cjs",
        "tailwind.config.cjs",
        "tailwind.css",
        "src/**/*.css",
        "src/**/*.tsx",
        "src/**/*.ts",
        "static/**"
      ],
      "outputs": ["storybook-static/**"]
    },
    "test:integration": {
      "outputs": ["coverage/**"]
    }
  }
}
