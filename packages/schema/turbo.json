{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      // Dedicated inputs for this package.
      "inputs": ["src", "codegen.ts", ".graphqlconfig"]
    }
  }
}
