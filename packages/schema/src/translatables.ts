import { TranslatableStringSource } from './generated/source';
export function mergeTranslatableStrings(
  strings: Array<TranslatableStringSource>,
): Array<TranslatableStringSource> {
  return strings;
}
