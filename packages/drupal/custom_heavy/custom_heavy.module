<?php

/**
 * @file
 */

use Drupal\Core\Form\FormStateInterface;

/**
 * Implements hook_module_implements_alter().
 */
function custom_heavy_module_implements_alter(&$implementations, $hook) {
  if ($hook == 'form_alter') {
    $group = $implementations['custom'];
    unset($implementations['custom']);
    $implementations['custom'] = $group;
  }
}

/**
 * Implements hook_form_alter().
 *
 * Executed at the very late stage.
 *
 * @see custom_heavy_module_implements_alter
 */
function custom_heavy_form_alter(&$form, FormStateInterface $form_state, $form_id) {
  // dpm($form_id);
}
